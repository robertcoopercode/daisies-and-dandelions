<div class="image-and-tags  container" data-section-id="{{ section.id }}">
  
  <h2 class="image-and-tags__title  section-title">{{ section.settings.title }}</h2>

    {% assign count = 0 %}
  
    <div class="image-and-tags__tags">
      {% for block in section.blocks %}
          <span class="tag" style="background-color: {{ section.settings.tag_color_1 }}" {{ block.shopify_attributes }}>
              {{ block.settings.text }}
            </span>
      {% endfor %}
    </div>
  
</div>

{% schema %}
  {
    "name": "Images with tags",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      },
      {
        "type": "color",
        "id": "tag_color_1",
        "label": "Tag color 1"
      }
    ],
    "blocks": [
      {
        "type": "tag",
        "name": "Tag",
        "settings": [
          {
            "id": "text",
            "type": "text",
            "label": "Tag"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Images with tags",
        "category": "Image"
      }
    ]
  }
{% endschema %}